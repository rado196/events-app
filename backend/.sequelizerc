const path = require('path');

const pathRoot = path.join(__dirname, 'src');
const pathDatabase = path.join(pathRoot, 'database');

module.exports = {
  'config': path.resolve(pathDatabase, 'config.js'),
  'seeders-path': path.resolve(pathDatabase, 'seeders'),
  'migrations-path': path.resolve(pathDatabase, 'migrations'),
  'models-path': path.resolve(pathRoot, 'models'),
};
